# r

TLDR page cloned from https://www.cheat-sheets.org/project/tldr/command/r/.

Upstream tldr pages are CC BY 4.0 (https://github.com/tldr-pages/tldr).

TLDR
Search
JavaScript must be enabled for this application.
Enter a command. For example:
shift
,
ffe
,
adguardhome
,
wormhole
.
R
R language interpreter.
More information:
https://www.r-project.org
.
Start a REPL (interactive shell):
R
Check R version:
R --version
Start R in vanilla mode (i.e. a blank session that doesn't save the workspace at the end):
R --vanilla
Execute a file:
R -f {{path/to/file.R}}
Execute an R expression and then exit:
R -e {{expr}}
Run R with a debugger:
R -d {{debugger}}
Check R packages from package sources:
R CMD check {{path/to/package_source}}
This is a
tldr pages
(
source
, CC BY 4.0) web wrapper for
cheat-sheets.org
.
All commands
,
popular commands
,
most used linux commands
.
Referrals
.
Progressive Web Application (PWA) version to install on your device
.
