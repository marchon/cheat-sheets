# sqlite3

TLDR page cloned from https://www.cheat-sheets.org/project/tldr/command/sqlite3/.

Upstream tldr pages are CC BY 4.0 (https://github.com/tldr-pages/tldr).

TLDR
Search
JavaScript must be enabled for this application.
Enter a command. For example:
pt
,
smbmap
,
minifab
,
ocaml
,
swagger codegen
.
sqlite3
The command-line interface to SQLite 3, which is a self-contained file-based embedded SQL engine.
More information:
https://sqlite.org
.
Start an interactive shell with a new database:
sqlite3
Open an interactive shell against an existing database:
sqlite3 {{path/to/database.sqlite3}}
Execute an SQL statement against a database and then exit:
sqlite3 {{path/to/database.sqlite3}} '{{SELECT * FROM some_table;}}'
This is a
tldr pages
(
source
, CC BY 4.0) web wrapper for
cheat-sheets.org
.
All commands
,
popular commands
,
most used linux commands
.
Referrals
.
Progressive Web Application (PWA) version to install on your device
.
